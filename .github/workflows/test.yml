name: Tests

on: push

jobs:
  test:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies and Build
        run: |
          npm i
          npm run build
      - name: Test
        run: |
          npm test
